# راهنمای نصب وابستگی‌های پروژه

## 🚀 نصب سریع (پیشنهادی)

### روش 1: استفاده از اسکریپت خودکار

1. **کلیک راست روی `SETUP_ALL.bat`** و انتخاب **"Run as Administrator"**
2. صبر کنید تا اسکریپت تمام وابستگی‌ها را نصب کند
3. در پایان، تمام موارد نصب شده‌اند و آماده استفاده هستند!

### روش 2: اجرای دستی PowerShell

```powershell
# باز کردن PowerShell به عنوان Administrator
# سپس اجرای:
powershell -ExecutionPolicy Bypass -File .\setup_dependencies.ps1
```

---

## 📋 چه چیزی نصب می‌شود؟

اسکریپت به صورت خودکار:

1. ✅ **Python 3.11+** را نصب می‌کند (اگر نصب نیست)
2. ✅ **Node.js 18+** را نصب می‌کند (اگر نصب نیست)
3. ✅ **Redis** را راه‌اندازی می‌کند (با Docker، اگر نصب است)
4. ✅ **Virtual Environment** را ایجاد می‌کند
5. ✅ **وابستگی‌های Python** را نصب می‌کند
6. ✅ **وابستگی‌های Node.js** را نصب می‌کند
7. ✅ **فایل .env** را ایجاد می‌کند
8. ✅ **Migrations** را اجرا می‌کند
9. ✅ **Superuser** را ایجاد می‌کند (username: admin, password: admin)

---

## ⚠️ نیازمندی‌ها

### ضروری:
- **دسترسی Administrator**: برای نصب Python و Node.js
- **اتصال به اینترنت**: برای دانلود پکیج‌ها

### اختیاری:
- **Docker Desktop**: برای راه‌اندازی Redis (اگر نصب نباشد، Redis راه‌اندازی نمی‌شود اما برنامه بدون Redis هم کار می‌کند)

---

## 🔍 بررسی نصب

پس از اجرای اسکریپت، می‌توانید بررسی کنید:

### بررسی Python
```powershell
python --version
# باید Python 3.11.x نمایش دهد
```

### بررسی Node.js
```powershell
node --version
# باید v18.x.x یا بالاتر نمایش دهد
```

### بررسی Redis
```powershell
Test-NetConnection -ComputerName localhost -Port 6379
# باید TcpTestSucceeded : True نمایش دهد
```

---

## 🎯 بعد از نصب

### 1. ویرایش فایل .env
فایل `.env` را باز کرده و API keys خود را اضافه کنید:

```env
# API Keys
TWELVEDATA_API_KEY=your-key-here
FINANCIALMODELINGPREP_API_KEY=your-key-here
GEMINI_API_KEY=your-key-here
# ... و سایر API keys
```

### 2. راه‌اندازی پروژه

**روش 1: استفاده از فایل آماده**
```batch
.\START_HERE.bat
```

**روش 2: دستی**
```powershell
# Terminal 1: Backend
cd backend
..\venv\Scripts\Activate.ps1
python manage.py runserver

# Terminal 2: Frontend
cd frontend
npm run dev
```

### 3. دسترسی به پروژه
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000/api/
- **Admin Panel**: http://localhost:8000/admin/
  - Username: `admin`
  - Password: `admin`

---

## ❌ عیب‌یابی

### مشکل: Python نصب نشد
**راه حل**:
1. Python را به صورت دستی از https://www.python.org/downloads/ نصب کنید
2. در هنگام نصب، گزینه "Add Python to PATH" را فعال کنید
3. Terminal را restart کنید
4. اسکریپت را دوباره اجرا کنید

### مشکل: Node.js نصب نشد
**راه حل**:
1. Node.js را به صورت دستی از https://nodejs.org/ نصب کنید
2. Terminal را restart کنید
3. اسکریپت را دوباره اجرا کنید

### مشکل: Redis راه‌اندازی نشد
**راه حل**:
1. Docker Desktop را نصب و راه‌اندازی کنید
2. Redis را به صورت دستی راه‌اندازی کنید:
   ```powershell
   docker run -d --name redis -p 6379:6379 redis:7-alpine
   ```
3. یا Redis را بدون Docker نصب کنید (اختیاری)

### مشکل: وابستگی‌های Python نصب نشدند
**راه حل**:
```powershell
cd backend
..\venv\Scripts\Activate.ps1
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

### مشکل: وابستگی‌های Node.js نصب نشدند
**راه حل**:
```powershell
cd frontend
npm cache clean --force
npm install --legacy-peer-deps
```

### مشکل: Migrations خطا می‌دهد
**راه حل**:
```powershell
cd backend
..\venv\Scripts\Activate.ps1
python manage.py makemigrations
python manage.py migrate
```

---

## 📝 نکات مهم

1. **دسترسی Administrator**: حتماً اسکریپت را با دسترسی Administrator اجرا کنید
2. **Restart Terminal**: پس از نصب Python یا Node.js، ممکن است نیاز به restart terminal باشد
3. **API Keys**: فایل .env را ویرایش کرده و API keys خود را اضافه کنید
4. **Redis**: Redis اختیاری است اما برای Celery (تسک‌های async) ضروری است
5. **Virtual Environment**: همیشه از virtual environment برای Python استفاده کنید

---

## 📚 مستندات بیشتر

برای اطلاعات بیشتر، فایل‌های زیر را مطالعه کنید:
- `تحلیل_وابستگی‌ها.md`: تحلیل کامل وابستگی‌ها
- `HOW_TO_RUN.md`: راهنمای اجرای پروژه
- `README.md`: مستندات کلی پروژه

---

## 🆘 پشتیبانی

اگر مشکلی داشتید:
1. لاگ‌های اسکریپت را بررسی کنید
2. فایل `تحلیل_وابستگی‌ها.md` را مطالعه کنید
3. بخش عیب‌یابی را بررسی کنید

---

**موفق باشید! 🎉**

