# راهنمای معاملات خودکار

## نحوه کار سیستم معاملات خودکار

سیستم معاملات خودکار به صورت **24/7** کار می‌کند و به طور خودکار بر اساس استراتژی‌های شما معامله می‌کند.

### ✅ شرایط لازم:

1. **MetaTrader 5 باید نصب و باز باشد**
   - MT5 باید روی همان سیستمی که سرور Django اجرا می‌شود نصب باشد
   - باید به حساب معاملاتی خود متصل باشید و لاگین باشید
   - MT5 باید همیشه باز باشد (نباید بسته شود)

2. **Redis باید در حال اجرا باشد** (برای Celery)
   ```bash
   redis-server
   ```

3. **Celery Worker و Celery Beat باید در حال اجرا باشند**
   ```bash
   # Terminal 1: Celery Worker
   celery -A config worker --loglevel=info
   
   # Terminal 2: Celery Beat (برای اجرای دوره‌ای)
   celery -A config beat --loglevel=info
   ```

4. **سرور Django باید در حال اجرا باشد**

### 🔄 نحوه کار:

1. **سیستم هر 5 دقیقه یکبار به صورت خودکار:**
   - داده‌های بازار را از MT5 دریافت می‌کند
   - استراتژی را روی داده‌ها اجرا می‌کند
   - سیگنال خرید/فروش را بررسی می‌کند
   - اگر سیگنال مناسب باشد و شرایط برقرار باشد، معامله را باز می‌کند

2. **بررسی شرایط:**
   - بازار باید باز باشد (24/5 به جز آخر هفته)
   - استراتژی باید پردازش شده و فعال باشد
   - تعداد معاملات باز باید کمتر از حداکثر باشد
   - سیگنال باید مناسب باشد (confidence > 0.5)

3. **مدیریت معاملات:**
   - قیمت‌های معاملات باز به‌روزرسانی می‌شود
   - Stop Loss و Take Profit به طور خودکار اعمال می‌شود
   - معاملات بسته شده در دیتابیس ثبت می‌شود

### ⚙️ تنظیمات معاملات خودکار:

برای هر استراتژی می‌توانید تنظیمات زیر را انجام دهید:

1. **فعال/غیرفعال کردن معامله خودکار**
2. **نماد معاملاتی** (پیش‌فرض: XAUUSD)
3. **حجم معامله** (به لات)
4. **حداکثر تعداد معاملات باز همزمان**
5. **فاصله زمانی بررسی** (هر چند دقیقه یکبار)
6. **استفاده از Stop Loss و Take Profit**
7. **مقدار Stop Loss و Take Profit (به پیپ)**
8. **ریسک در هر معامله (به درصد)**

### 📝 مراحل راه‌اندازی:

#### 1. ایجاد Migration و اجرای آن:
```bash
cd backend
python manage.py makemigrations
python manage.py migrate
```

#### 2. اجرای Redis:
```bash
redis-server
```

#### 3. اجرای Celery Worker (در Terminal جداگانه):
```bash
cd backend
celery -A config worker --loglevel=info
```

#### 4. اجرای Celery Beat (در Terminal جداگانه):
```bash
cd backend
celery -A config beat --loglevel=info
```

#### 5. اجرای سرور Django:
```bash
cd backend
python manage.py runserver
```

#### 6. در رابط کاربری:
1. به صفحه "معاملات زنده" بروید
2. یک استراتژی که پردازش شده است انتخاب کنید
3. تنظیمات معامله خودکار را پیکربندی کنید
4. معامله خودکار را فعال کنید

### 🔍 تست سیستم:

قبل از فعال کردن معامله خودکار با سرمایه واقعی:

1. از دکمه "تست سیگنال" استفاده کنید تا ببینید استراتژی چه سیگنالی تولید می‌کند
2. در حساب Demo تست کنید
3. با حجم کوچک شروع کنید

### ⚠️ نکات مهم:

1. **MT5 باید همیشه باز باشد** - اگر MT5 بسته شود، سیستم نمی‌تواند معامله کند
2. **سرور باید همیشه روشن باشد** - برای معاملات 24/7
3. **Redis باید در حال اجرا باشد** - برای Celery
4. **Celery Beat باید در حال اجرا باشد** - برای اجرای دوره‌ای task ها
5. **بازار باید باز باشد** - سیستم خودکار بررسی می‌کند که بازار باز باشد

### 🚀 اجرای روی سرور (Production):

برای اجرای روی سرور که همیشه روشن است، می‌توانید از systemd یا supervisor استفاده کنید:

**مثال systemd service برای Celery Worker:**
```ini
[Unit]
Description=Celery Worker
After=network.target

[Service]
Type=forking
User=your_user
WorkingDirectory=/path/to/backend
ExecStart=/path/to/venv/bin/celery -A config worker --loglevel=info --detach
ExecStop=/bin/kill -s TERM $MAINPID

[Install]
WantedBy=multi-user.target
```

**مثال systemd service برای Celery Beat:**
```ini
[Unit]
Description=Celery Beat
After=network.target

[Service]
Type=forking
User=your_user
WorkingDirectory=/path/to/backend
ExecStart=/path/to/venv/bin/celery -A config beat --loglevel=info --detach
ExecStop=/bin/kill -s TERM $MAINPID

[Install]
WantedBy=multi-user.target
```

### 📊 مانیتورینگ:

- لاگ‌های Celery را بررسی کنید
- در صفحه "معاملات زنده" می‌توانید معاملات را مشاهده کنید
- در Admin Panel می‌توانید تنظیمات و معاملات را مدیریت کنید

### ❓ سوالات متداول:

**Q: آیا می‌توانم معامله خودکار را متوقف کنم؟**
A: بله، در تنظیمات استراتژی می‌توانید `is_enabled` را به `false` تغییر دهید.

**Q: اگر سرور خاموش شود چه می‌شود؟**
A: معاملات باز در MT5 باقی می‌مانند، اما سیستم نمی‌تواند معامله جدیدی باز کند.

**Q: چگونه می‌توانم فاصله زمانی بررسی را تغییر دهم؟**
A: در تنظیمات Auto Trading Settings می‌توانید `check_interval_minutes` را تغییر دهید.

**Q: آیا می‌توانم چند استراتژی را به صورت همزمان فعال کنم؟**
A: بله، می‌توانید برای هر استراتژی تنظیمات جداگانه تعریف کنید.

