# راهنمای آپلود پروژه به GitHub

## ✅ مراحل انجام شده

1. ✅ Git repository ایجاد شد
2. ✅ فایل `.gitignore` به‌روزرسانی شد
3. ✅ فایل‌های cache و media از Git حذف شدند
4. ✅ اولین commit ایجاد شد

## 📋 مراحل بعدی برای آپلود به GitHub

### مرحله 1: ایجاد Repository در GitHub

1. به [GitHub.com](https://github.com) بروید و وارد حساب کاربری خود شوید
2. روی دکمه **"+"** در بالای صفحه کلیک کنید و **"New repository"** را انتخاب کنید
3. نام repository را وارد کنید (مثلاً: `Smart-AI-Trading-Strategy-Optimizer`)
4. توضیحات را وارد کنید (اختیاری)
5. **Public** یا **Private** را انتخاب کنید
6. **⚠️ مهم**: تیک **"Initialize this repository with a README"** را **نزنید** (چون ما قبلاً README داریم)
7. روی **"Create repository"** کلیک کنید

### مرحله 2: اتصال Repository محلی به GitHub

بعد از ایجاد repository در GitHub، دستورات زیر را در PowerShell اجرا کنید:

```powershell
cd "C:\Users\393\Desktop\Smart AI Trading Strategy Optimizer"

# اضافه کردن remote repository (URL را با URL واقعی repository خود جایگزین کنید)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY_NAME.git

# تغییر نام branch به main (اگر GitHub از main استفاده می‌کند)
git branch -M main

# آپلود کد به GitHub
git push -u origin main
```

**نکته**: `YOUR_USERNAME` و `YOUR_REPOSITORY_NAME` را با اطلاعات واقعی خود جایگزین کنید.

### مرحله 3: تأیید

بعد از اجرای دستورات، به صفحه repository در GitHub بروید و مطمئن شوید که تمام فایل‌ها آپلود شده‌اند.

## 🔐 استفاده از Personal Access Token (اگر لازم باشد)

اگر GitHub از شما رمز عبور خواست، باید از **Personal Access Token** استفاده کنید:

1. به GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) بروید
2. روی **"Generate new token"** کلیک کنید
3. نام token را وارد کنید
4. دسترسی‌های لازم را انتخاب کنید (حداقل `repo`)
5. روی **"Generate token"** کلیک کنید
6. Token را کپی کنید (فقط یک بار نمایش داده می‌شود)
7. هنگام push، به جای رمز عبور، از این token استفاده کنید

## 📝 دستورات مفید Git

```powershell
# مشاهده وضعیت فایل‌ها
git status

# اضافه کردن فایل‌های جدید
git add .

# ایجاد commit
git commit -m "توضیحات تغییرات"

# آپلود به GitHub
git push

# دریافت آخرین تغییرات از GitHub
git pull

# مشاهده تاریخچه commits
git log
```

## ⚠️ نکات مهم

1. **فایل‌های حساس**: مطمئن شوید که فایل `.env` در `.gitignore` است (قبلاً اضافه شده)
2. **فایل‌های بزرگ**: فایل‌های cache و media از Git حذف شده‌اند
3. **رمزهای عبور**: هرگز API keys یا رمزهای عبور را در کد commit نکنید

## 🎉 موفق باشید!

بعد از آپلود، پروژه شما در GitHub در دسترس خواهد بود.

