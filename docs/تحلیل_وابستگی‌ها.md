# تحلیل کامل وابستگی‌های پروژه

## 📋 خلاصه وابستگی‌ها

این پروژه یک سیستم مدیریت استراتژی‌های معاملاتی با هوش مصنوعی است که نیاز به وابستگی‌های زیر دارد:

### 1. وابستگی‌های سیستم (System Dependencies)

#### Python 3.11+
- **ضروری**: بله
- **توضیحات**: برای اجرای Backend Django
- **نصب**: از https://www.python.org/downloads/ یا با استفاده از `winget install Python.Python.3.11`
- **بررسی**: `python --version`

#### Node.js 18+
- **ضروری**: بله
- **توضیحات**: برای اجرای Frontend React
- **نصب**: از https://nodejs.org/ یا با استفاده از `winget install OpenJS.NodeJS.LTS`
- **بررسی**: `node --version`

#### Redis (اختیاری)
- **ضروری**: خیر (برای Celery)
- **توضیحات**: برای پردازش تسک‌های async (Celery Worker/Beat)
- **نصب**: 
  - با Docker: `docker run -d --name redis -p 6379:6379 redis:7-alpine`
  - یا از https://github.com/tporadowski/redis/releases
- **بررسی**: `Test-NetConnection -ComputerName localhost -Port 6379`

#### PostgreSQL (اختیاری)
- **ضروری**: خیر (SQLite برای development کافی است)
- **توضیحات**: برای Production
- **نصب**: از https://www.postgresql.org/download/windows/

#### Docker (اختیاری)
- **ضروری**: خیر (فقط برای Redis و Docker Compose)
- **توضیحات**: برای اجرای Redis و سایر سرویس‌ها
- **نصب**: از https://www.docker.com/products/docker-desktop

---

### 2. وابستگی‌های Python (Backend)

تمام وابستگی‌ها در فایل `backend/requirements.txt` تعریف شده‌اند:

#### Core Framework
- **django==5.1.2**: Framework اصلی
- **djangorestframework==3.15.2**: API REST
- **django-cors-headers==4.4.0**: مدیریت CORS
- **gunicorn==23.0.0**: WSGI Server برای Production
- **whitenoise==6.7.0**: سرو فایل‌های استاتیک

#### Task Queue
- **celery==5.4.0**: پردازش تسک‌های async
- **redis==5.1.1**: کلاینت Redis برای Celery

#### Data Processing
- **pandas==2.2.3**: پردازش داده‌های مالی
- **numpy==2.1.2**: محاسبات عددی

#### API Integration
- **requests==2.32.3**: درخواست‌های HTTP
- **google-generativeai==0.8.2**: Google Gemini AI
- **google-auth==2.35.0**: احراز هویت Google
- **google-auth-httplib2==0.2.0**: HTTP Library برای Google Auth
- **kavenegar==1.1.2**: سرویس پیامک

#### File Processing
- **Pillow==10.4.0**: پردازش تصاویر
- **python-docx==0.8.11**: پردازش فایل‌های Word
- **pypdf==4.3.1**: پردازش فایل‌های PDF

#### MetaTrader 5
- **MetaTrader5==5.0.5388**: اتصال به MetaTrader 5

#### Utilities
- **python-dotenv==1.0.1**: مدیریت متغیرهای محیطی

#### Optional (در requirements.txt کامنت شده)
- **optuna==3.5.0**: بهینه‌سازی Bayesian (اختیاری)
- **torch==2.3.1**: Deep Learning (اختیاری)

---

### 3. وابستگی‌های Node.js (Frontend)

تمام وابستگی‌ها در فایل `frontend/package.json` تعریف شده‌اند:

#### Core
- **react==^18.2.0**: Framework اصلی
- **react-dom==^18.2.0**: رندر React
- **react-router-dom==^6.20.0**: مسیریابی

#### HTTP Client
- **axios==^1.6.2**: درخواست‌های HTTP

#### Charts
- **chart.js==^4.4.0**: کتابخانه نمودار
- **react-chartjs-2==^5.2.0**: رپر React برای Chart.js

#### Development Tools
- **typescript==^5.2.2**: TypeScript
- **vite==^5.0.8**: Build Tool
- **@vitejs/plugin-react==^4.2.1**: پلاگین React برای Vite
- **tailwindcss==^3.3.6**: CSS Framework
- **postcss==^8.4.32**: پردازش CSS
- **autoprefixer==^10.4.16**: افزودن prefix به CSS

#### Linting
- **eslint==^8.55.0**: Linter
- **@typescript-eslint/eslint-plugin==^6.14.0**: ESLint Plugin برای TypeScript
- **@typescript-eslint/parser==^6.14.0**: Parser برای TypeScript
- **eslint-plugin-react-hooks==^4.6.0**: ESLint Plugin برای React Hooks
- **eslint-plugin-react-refresh==^0.4.5**: ESLint Plugin برای React Refresh

#### Type Definitions
- **@types/react==^18.2.43**: Type Definitions برای React
- **@types/react-dom==^18.2.17**: Type Definitions برای React DOM

---

### 4. سرویس‌های خارجی (External Services)

#### API Keys مورد نیاز (در فایل .env تنظیم می‌شوند):

1. **TwelveData API**: داده‌های بازار
2. **FinancialModelingPrep API**: داده‌های مالی
3. **MetalsAPI API**: قیمت طلا و فلزات
4. **OANDA API**: داده‌های Forex
5. **Google Gemini API**: تحلیل هوش مصنوعی
6. **Kavenegar API**: ارسال پیامک
7. **Google OAuth**: احراز هویت (اختیاری)
8. **reCAPTCHA**: محافظت از bot (اختیاری)

---

## 🚀 نصب خودکار وابستگی‌ها

### روش 1: استفاده از اسکریپت خودکار (پیشنهادی)

1. **اجرای اسکریپت با دسترسی Administrator**:
   ```powershell
   # روش 1: اجرای فایل BAT
   .\SETUP_ALL.bat
   
   # روش 2: اجرای مستقیم PowerShell
   powershell -ExecutionPolicy Bypass -File .\setup_dependencies.ps1
   ```

2. **اسکریپت به صورت خودکار**:
   - Python 3.11+ را نصب می‌کند (اگر نصب نیست)
   - Node.js 18+ را نصب می‌کند (اگر نصب نیست)
   - Redis را راه‌اندازی می‌کند (اگر Docker نصب است)
   - Virtual environment را ایجاد می‌کند
   - وابستگی‌های Python را نصب می‌کند
   - وابستگی‌های Node.js را نصب می‌کند
   - فایل .env را ایجاد می‌کند
   - Migrations را اجرا می‌کند
   - Superuser را ایجاد می‌کند

### روش 2: نصب دستی

#### مرحله 1: نصب Python
```powershell
# با winget
winget install Python.Python.3.11

# یا دانلود از python.org
# https://www.python.org/downloads/
```

#### مرحله 2: نصب Node.js
```powershell
# با winget
winget install OpenJS.NodeJS.LTS

# یا دانلود از nodejs.org
# https://nodejs.org/
```

#### مرحله 3: نصب وابستگی‌های Python
```powershell
cd backend
python -m venv ..\venv
..\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

#### مرحله 4: نصب وابستگی‌های Node.js
```powershell
cd frontend
npm install
```

#### مرحله 5: تنظیم .env
```powershell
# کپی کردن env.example به .env
copy env.example .env
# ویرایش .env و افزودن API keys
```

#### مرحله 6: اجرای Migrations
```powershell
cd backend
python manage.py migrate
python manage.py createsuperuser
```

---

## ✅ بررسی نصب وابستگی‌ها

### بررسی Python
```powershell
python --version
# باید Python 3.11.x نمایش دهد
```

### بررسی Node.js
```powershell
node --version
# باید v18.x.x یا بالاتر نمایش دهد
```

### بررسی pip
```powershell
pip --version
```

### بررسی npm
```powershell
npm --version
```

### بررسی Redis
```powershell
Test-NetConnection -ComputerName localhost -Port 6379
# باید TcpTestSucceeded : True نمایش دهد
```

### بررسی وابستگی‌های Python
```powershell
cd backend
..\venv\Scripts\Activate.ps1
pip list
# باید تمام پکیج‌های requirements.txt را نمایش دهد
```

### بررسی وابستگی‌های Node.js
```powershell
cd frontend
npm list --depth=0
# باید تمام پکیج‌های package.json را نمایش دهد
```

---

## 🔧 عیب‌یابی

### مشکل: Python یافت نشد
**راه حل**:
1. Python را از python.org نصب کنید
2. در هنگام نصب، گزینه "Add Python to PATH" را فعال کنید
3. Terminal را restart کنید

### مشکل: Node.js یافت نشد
**راه حل**:
1. Node.js را از nodejs.org نصب کنید
2. Terminal را restart کنید

### مشکل: pip install خطا می‌دهد
**راه حل**:
```powershell
python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

### مشکل: npm install خطا می‌دهد
**راه حل**:
```powershell
npm cache clean --force
npm install --legacy-peer-deps
```

### مشکل: Redis متصل نمی‌شود
**راه حل**:
1. Docker Desktop را راه‌اندازی کنید
2. Redis را با Docker راه‌اندازی کنید:
   ```powershell
   docker run -d --name redis -p 6379:6379 redis:7-alpine
   ```
3. یا Redis را به صورت دستی نصب کنید

### مشکل: Migrations خطا می‌دهد
**راه حل**:
```powershell
cd backend
python manage.py makemigrations
python manage.py migrate
```

---

## 📝 نکات مهم

1. **دسترسی Administrator**: برای نصب Python و Node.js نیاز به دسترسی Administrator است
2. **PATH**: پس از نصب Python و Node.js، ممکن است نیاز به restart terminal باشد
3. **Virtual Environment**: همیشه از virtual environment برای Python استفاده کنید
4. **API Keys**: فایل .env را ویرایش کرده و API keys خود را اضافه کنید
5. **Redis**: Redis اختیاری است اما برای Celery (تسک‌های async) ضروری است
6. **PostgreSQL**: برای development از SQLite استفاده می‌شود (نیازی به نصب PostgreSQL نیست)

---

## 🎯 مراحل بعدی

پس از نصب وابستگی‌ها:

1. **ویرایش فایل .env**: API keys خود را اضافه کنید
2. **راه‌اندازی Backend**: `cd backend && python manage.py runserver`
3. **راه‌اندازی Frontend**: `cd frontend && npm run dev`
4. **راه‌اندازی Redis** (اگر نیاز است): `docker run -d --name redis -p 6379:6379 redis:7-alpine`
5. **راه‌اندازی Celery** (اگر نیاز است): `cd backend && celery -A config worker --loglevel=info`

---

## 📚 منابع بیشتر

- [Python Documentation](https://docs.python.org/3/)
- [Django Documentation](https://docs.djangoproject.com/)
- [React Documentation](https://react.dev/)
- [Node.js Documentation](https://nodejs.org/docs)
- [Redis Documentation](https://redis.io/docs/)

