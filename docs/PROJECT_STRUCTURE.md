# ساختار پروژه Smart AI Trading Strategy Optimizer

## 📁 ساختار کلی

```
SmartAITradingStrategyOptimizer/
├── backend/                 # Backend Django Application
│   ├── ai_module/          # ماژول هوش مصنوعی و پردازش استراتژی
│   ├── api/                # API endpoints و views
│   ├── cache/              # فایل‌های کش (در .gitignore)
│   ├── config/             # تنظیمات Django
│   ├── core/               # مدل‌های اصلی و migrations
│   ├── logs/               # فایل‌های لاگ (در .gitignore)
│   ├── media/              # فایل‌های آپلود شده (در .gitignore)
│   ├── staticfiles/        # فایل‌های استاتیک (در .gitignore)
│   ├── tests/              # تمام تست‌های پروژه
│   ├── manage.py           # Django management script
│   └── requirements.txt    # وابستگی‌های Python
│
├── frontend/               # Frontend React Application
│   ├── src/                # کدهای منبع React
│   ├── public/             # فایل‌های عمومی
│   └── package.json        # وابستگی‌های Node.js
│
├── docs/                   # تمام مستندات پروژه
│   ├── *.md                # فایل‌های راهنما و مستندات
│   └── *.txt               # فایل‌های متنی
│
├── scripts/                # تمام اسکریپت‌های اجرایی
│   ├── *.ps1               # اسکریپت‌های PowerShell
│   ├── *.bat               # اسکریپت‌های Batch
│   └── *.py                # اسکریپت‌های Python کمکی
│
├── docker-compose.yml      # تنظیمات Docker Compose
├── Dockerfile.backend      # Dockerfile برای Backend
├── Dockerfile.frontend     # Dockerfile برای Frontend
├── nginx.conf              # تنظیمات Nginx
├── env.example             # نمونه فایل Environment
├── README.md               # راهنمای اصلی پروژه
└── .gitignore              # فایل‌های نادیده گرفته شده
```

## 📂 توضیحات پوشه‌ها

### backend/
پوشه اصلی Backend که شامل:
- **ai_module/**: ماژول‌های هوش مصنوعی، NLP parser، و optimizer
- **api/**: تمام API endpoints، views، serializers
- **config/**: تنظیمات Django (settings.py, urls.py, wsgi.py, asgi.py)
- **core/**: مدل‌های اصلی دیتابیس و migrations
- **tests/**: تمام فایل‌های تست (test_*.py)

### frontend/
پوشه Frontend React با TypeScript:
- **src/**: کدهای منبع اصلی
- **public/**: فایل‌های استاتیک عمومی

### docs/
تمام مستندات پروژه شامل:
- راهنماهای نصب و راه‌اندازی
- راهنماهای تنظیمات
- مستندات API
- راهنماهای فارسی و انگلیسی

### scripts/
تمام اسکریپت‌های اجرایی:
- اسکریپت‌های راه‌اندازی (start_*.ps1, START_*.bat)
- اسکریپت‌های تنظیمات (setup_*.ps1, SETUP_*.bat)
- اسکریپت‌های استقرار (deploy.ps1)
- اسکریپت‌های کمکی (check_*.ps1, test_*.ps1)

## 🔧 فایل‌های مهم

### فایل‌های ریشه
- `README.md`: راهنمای اصلی پروژه
- `docker-compose.yml`: تنظیمات Docker برای اجرای محلی
- `env.example`: نمونه فایل environment variables
- `.gitignore`: فایل‌های نادیده گرفته شده توسط Git

### Backend
- `backend/manage.py`: Django management script
- `backend/requirements.txt`: وابستگی‌های Python
- `backend/config/settings.py`: تنظیمات اصلی Django

### Frontend
- `frontend/package.json`: وابستگی‌های Node.js
- `frontend/vite.config.ts`: تنظیمات Vite
- `frontend/tsconfig.json`: تنظیمات TypeScript

## 🚀 اجرای پروژه

برای اجرای پروژه، از اسکریپت‌های موجود در پوشه `scripts/` استفاده کنید:

```powershell
# راه‌اندازی کامل
.\scripts\START_ALL.ps1

# یا راه‌اندازی جداگانه
.\scripts\start_backend.ps1
.\scripts\start_frontend.ps1
```

برای اطلاعات بیشتر، به `docs/HOW_TO_RUN.md` مراجعه کنید.

## 📝 تغییرات ساختار

این ساختار جدید در تاریخ 2025-11-17 ایجاد شده است تا:
- ✅ فایل‌های مستندات در یک مکان متمرکز باشند
- ✅ اسکریپت‌های اجرایی سازمان‌دهی شده باشند
- ✅ تست‌ها در پوشه مخصوص خود قرار گیرند
- ✅ ساختار پروژه تمیز و قابل نگهداری باشد

