# ⚡ راهنمای سریع: تنظیم DDNS

## 🎯 هدف
دسترسی به سرور از طریق دامنه عمومی (مثلاً `myforex.duckdns.org`) به جای IP

---

## 🚀 راهنمای سریع DuckDNS (3 دقیقه)

### 1. ثبت‌نام
- به [duckdns.org](https://www.duckdns.org/) بروید
- با Google/GitHub وارد شوید
- یک دامنه انتخاب کنید (مثلاً `myforex`)
- Token را کپی کنید

### 2. تنظیم در پنل ادمین
1. به پنل ادمین بروید: `http://YOUR_IP:8000/admin/`
2. وارد شوید (admin/admin)
3. **DDNS Configurations** > **Add DDNS Configuration**
4. وارد کنید:
   - Provider: **DuckDNS (رایگان)**
   - Domain: `myforex` (بدون .duckdns.org)
   - Token: Token کپی شده
   - Is Enabled: ✅
5. **Save** کنید

### 3. تست
- در صفحه Profile > بخش DDNS Configuration (فقط ادمین)
- یا از API: `POST /api/ddns-configurations/{id}/test/`

---

## ✅ دسترسی

بعد از تنظیم، می‌توانید از این آدرس‌ها استفاده کنید:
- Frontend: `http://myforex.duckdns.org:3000`
- Backend: `http://myforex.duckdns.org:8000`
- Admin: `http://myforex.duckdns.org:8000/admin/`

---

## ⚠️ نکات مهم

1. **Port Forwarding**: پورت‌های 8000 و 3000 را در Router باز کنید
2. **Celery Beat**: باید در حال اجرا باشد (برای به‌روزرسانی خودکار)
3. **فقط یک تنظیمات فعال**: فقط یک DDNS می‌تواند فعال باشد

---

## 📖 راهنمای کامل
فایل `راهنمای_تنظیم_DDNS.md` را مطالعه کنید.

