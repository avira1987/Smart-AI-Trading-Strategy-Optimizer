# 🚀 نحوه راه‌اندازی پروژه

## روش ساده (استفاده از فایل آماده)

### روی فایل زیر کلیک کنید:
```
START_HERE.bat
```

این فایل به صورت خودکار backend و frontend را راه‌اندازی می‌کند.

---

## یا به صورت دستی:

### مرحله 1: راه‌اندازی Backend

1. یک ترمینال PowerShell باز کنید
2. دستورات زیر را وارد کنید:

```powershell
cd "C:\Users\393\Desktop\Smart AI Trading Strategy Optimizer"
cd backend
python manage.py runserver
```

بگذارید این terminal باز بماند.

### مرحله 2: راه‌اندازی Frontend

1. **یک ترمینال جدید** PowerShell باز کنید
2. دستورات زیر را وارد کنید:

```powershell
cd "C:\Users\393\Desktop\Smart AI Trading Strategy Optimizer"
cd frontend
npm run dev
```

---

## ✅ دسترسی

پس از راه‌اندازی، به این آدرس‌ها بروید:

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000/api/
- **Admin Panel**: http://localhost:8000/admin/
  - Username: `admin`
  - Password: `admin`

---

## 🔴 متوقف کردن

برای متوقف کردن سرورها:
1. در هر دو terminal، کلید `Ctrl + C` را فشار دهید
2. یا Task Manager باز کنید و `node.exe` و `python.exe` را متوقف کنید

---

## ⚠️ مشکلات احتمالی

### اگر صفحه سفید یا خطا بدهد:

1. **Console مرورگر را باز کنید** (F12)
2. **خطاها را بخوانید**
3. **با من به اشتراک بگذارید**

### اگر پورت‌ها اشغال هستند:

```powershell
# متوقف کردن همه node
taskkill /F /IM node.exe

# متوقف کردن همه python  
taskkill /F /IM python.exe
```

---

## 📝 نکات مهم

- Backend روی پورت **8000** اجرا می‌شود
- Frontend روی پورت **3000** اجرا می‌شود
- باید **دو ترمینال جداگانه** باز داشته باشید
- هر دو ترمینال را **باز نگه دارید**

