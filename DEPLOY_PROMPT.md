# 🚀 Prompt برای Deployment خودکار در Cursor

این prompt را در Cursor کپی کنید تا پروژه را از GitHub دریافت کرده و روی سرور راه‌اندازی کنید.

---

## 📋 Prompt برای Cursor:

```
من می‌خواهم پروژه Smart AI Trading Strategy Optimizer را از GitHub دریافت کرده و روی سرور راه‌اندازی کنم.

لطفاً این مراحل را انجام دهید:

1. Clone پروژه از GitHub:
   Repository: https://github.com/avira1987/Smart-AI-Trading-Strategy-Optimizer.git
   Branch: main

2. بررسی و نصب وابستگی‌ها:
   - Python 3.11+ و pip
   - Node.js 18+ و npm
   - PostgreSQL (اختیاری - می‌توان از SQLite استفاده کرد)
   - Redis (یا Docker برای Redis)

3. تنظیم Environment Variables:
   - از فایل env.example یک .env ایجاد کن
   - DEBUG=False تنظیم کن
   - ENV=PRODUCTION تنظیم کن
   - SECRET_KEY را با یک کلید امنیتی تصادفی جایگزین کن
   - تمام API keys را از کاربر بپرس و تنظیم کن

4. نصب وابستگی‌های Python:
   - ایجاد virtual environment
   - نصب requirements.txt

5. نصب وابستگی‌های Node.js:
   - نصب package.json

6. راه‌اندازی Database:
   - اجرای migrations
   - ایجاد superuser (اختیاری)

7. Build Frontend:
   - اجرای npm run build

8. راه‌اندازی سرویس‌ها:
   - Backend با Gunicorn روی پورت 8000
   - Frontend با serve یا nginx روی پورت 3000
   - Celery Worker
   - Celery Beat

9. بررسی وضعیت:
   - تست اتصال به Backend API
   - تست اتصال به Frontend
   - نمایش آدرس‌های دسترسی

لطفاً تمام مراحل را به صورت خودکار انجام دهید و در هر مرحله که نیاز به اطلاعات کاربر است (مثل API keys) از من بپرسید.
```

---

## 🎯 استفاده:

1. این prompt را کپی کنید
2. در Cursor باز کنید
3. Paste کنید و Enter بزنید
4. Cursor به صورت خودکار تمام مراحل را انجام می‌دهد

---

## 📝 اطلاعات مورد نیاز:

قبل از اجرا، این اطلاعات را آماده کنید:
- API Keys (TwelveData, Gemini, MetalsAPI, etc.)
- Database credentials (اگر از PostgreSQL استفاده می‌کنید)
- Redis URL (اگر خارج از Docker است)
- SECRET_KEY (یا اجازه دهید Cursor یک کلید تصادفی ایجاد کند)

---

## ✅ خروجی مورد انتظار:

بعد از اجرای موفق:
- ✅ پروژه از GitHub clone شده
- ✅ تمام وابستگی‌ها نصب شده
- ✅ Environment variables تنظیم شده
- ✅ Database آماده است
- ✅ Frontend build شده
- ✅ تمام سرویس‌ها در حال اجرا هستند
- ✅ آدرس‌های دسترسی نمایش داده شده

---

## 🔗 آدرس‌های دسترسی:

- **Backend API**: http://YOUR_SERVER_IP:8000
- **Frontend**: http://YOUR_SERVER_IP:3000
- **Admin Panel**: http://YOUR_SERVER_IP:8000/admin

---

**نکته:** این prompt برای استفاده در Cursor طراحی شده و تمام مراحل را به صورت خودکار انجام می‌دهد.

