# راهنمای رفع مشکل تست استراتژی

## 🔍 مشکلات رایج

### مشکل 1: "No data providers configured"

**علت:** API key تنظیم نشده است.

**راه حل:**
1. API key پیش‌فرض در کد تنظیم شده است
2. اگر هنوز کار نمی‌کند، در فایل `.env` اضافه کنید:
   ```bash
   FINANCIALMODELINGPREP_API_KEY=YOUR_API_KEY_HERE
   ```
3. Backend را restart کنید

### مشکل 2: در ویندوز با MT4 یا نسخه قدیمی MT5 کار نمی‌کند

**علت:** سیستم فقط MT5 را چک می‌کند.

**راه حل:**
- سیستم اکنون به طور خودکار از API های خارجی استفاده می‌کند
- حتی اگر MT4 نصب باشد، سیستم از Financial Modeling Prep استفاده می‌کند
- MT5/MT4 **اختیاری** است و فقط به عنوان آخرین راه استفاده می‌شود

### مشکل 3: در موبایل کار نمی‌کند

**علت:** ممکن است API key در سرور تنظیم نشده باشد.

**راه حل:**
1. API key باید در **سرور** تنظیم شود (نه در موبایل)
2. در فایل `.env` در سرور اضافه کنید:
   ```bash
   FINANCIALMODELINGPREP_API_KEY=YOUR_API_KEY_HERE
   ```
3. Backend را restart کنید

## ✅ تست سریع

برای تست اینکه آیا سیستم بدون MT5 کار می‌کند:

```bash
cd backend
python test_backtest_without_mt5.py
```

این تست:
- ✅ بررسی می‌کند که API keys در دسترس هستند
- ✅ تست می‌کند که می‌توان داده دریافت کرد
- ✅ تست می‌کند که قیمت لحظه‌ای کار می‌کند
- ✅ تست می‌کند که جریان کامل بک‌تست کار می‌کند

## 🔧 تنظیمات خودکار

سیستم به طور خودکار:
1. ✅ اگر API key در `.env` نباشد، از API key پیش‌فرض استفاده می‌کند
2. ✅ اول از Financial Modeling Prep استفاده می‌کند
3. ✅ اگر کار نکرد، از Twelve Data استفاده می‌کند
4. ✅ فقط در آخر به MT5 fallback می‌کند (اگر نصب باشد)

## 📱 برای کاربران موبایل

**هیچ تنظیمات خاصی نیاز نیست!**

فقط:
1. از مرورگر موبایل به سایت بروید
2. استراتژی را آپلود کنید
3. بک‌تست را اجرا کنید

سیستم به طور خودکار از API های تنظیم شده استفاده می‌کند.

## 🖥️ برای کاربران Windows با MT4

**نیازی به نصب MT5 نیست!**

سیستم به طور خودکار:
- ✅ از Financial Modeling Prep استفاده می‌کند
- ✅ MT4 را نادیده می‌گیرد
- ✅ بدون نیاز به MT5 کار می‌کند

## 🐛 عیب‌یابی

### بررسی API Keys

```bash
cd backend
python -c "import os; from dotenv import load_dotenv; load_dotenv(); print('FMP:', 'OK' if os.getenv('FINANCIALMODELINGPREP_API_KEY') else 'NOT SET')"
```

### تست مستقیم API

```bash
cd backend
python test_backtest_without_mt5.py
```

### بررسی لاگ‌ها

```bash
# Windows
type backend\logs\api.log

# Linux/Mac
tail -f backend/logs/api.log
```

## 📝 نکات مهم

1. **API key در سرور تنظیم می‌شود** (نه در موبایل)
2. **MT5/MT4 اختیاری است** - سیستم بدون آن هم کار می‌کند
3. **API key پیش‌فرض** در کد تنظیم شده است (برای تست)
4. **برای production**، از API key خودتان استفاده کنید

## ✅ نتیجه

اکنون سیستم:
- ✅ برای کاربران موبایل کار می‌کند
- ✅ برای Windows بدون MT5 کار می‌کند
- ✅ برای Windows با MT4 کار می‌کند
- ✅ برای Windows با نسخه قدیمی MT5 کار می‌کند
- ✅ نیازی به نصب نرم‌افزار خاصی نیست

**فقط کافی است از مرورگر استفاده کنید!** 🎉

