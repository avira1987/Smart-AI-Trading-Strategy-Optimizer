# راهنمای تست استراتژی بدون MetaTrader 5

این راهنما برای کاربرانی است که **Windows ندارند** یا **MetaTrader 5 نصب نشده** است (مثل کاربران موبایل، Linux، Mac).

## ✅ آیا تست استراتژی بدون MT5 امکان‌پذیر است؟

**بله!** سیستم اکنون به طور کامل از API های خارجی استفاده می‌کند و **نیازی به MT5 ندارد**.

## 🔧 تنظیمات مورد نیاز

### 1. تنظیم API Key

برای تست استراتژی، حداقل یکی از API keys زیر را باید تنظیم کنید:

#### Financial Modeling Prep (توصیه می‌شود)
```bash
# در فایل .env
FINANCIALMODELINGPREP_API_KEY=CrFA9qczl3MRwERIiCGcmqloOilqkOBY
```

#### Twelve Data (جایگزین)
```bash
# در فایل .env
TWELVEDATA_API_KEY=your_twelvedata_api_key_here
```

### 2. اولویت استفاده از Providers

سیستم به ترتیب زیر از providers استفاده می‌کند:

1. **Financial Modeling Prep** (اولویت اول)
2. **Twelve Data** (در صورت عدم دسترسی به Financial Modeling Prep)
3. **Alpha Vantage** (در صورت وجود API key)
4. **OANDA** (در صورت وجود API key)
5. **MetalsAPI** (در صورت وجود API key)
6. **MT5** (فقط به عنوان آخرین راه و فقط اگر نصب باشد)

## 🎯 نحوه کار

### برای کاربران موبایل/بدون MT5:

1. **آپلود استراتژی**: از طریق مرورگر موبایل
2. **اجرای بک‌تست**: 
   - انتخاب استراتژی
   - تنظیم پارامترها
   - کلیک روی "Run Backtest"
3. **دریافت نتایج**: 
   - سیستم از API های تنظیم شده داده می‌گیرد
   - بک‌تست اجرا می‌شود
   - نتایج نمایش داده می‌شود

### جریان کار:

```
کاربر موبایل → مرورگر → API سرور
    ↓
1. درخواست بک‌تست
    ↓
2. سیستم بررسی می‌کند:
   - آیا API key تنظیم شده؟
   - کدام provider در دسترس است؟
    ↓
3. دریافت داده از API:
   - Financial Modeling Prep
   - یا Twelve Data
   - یا سایر providers
    ↓
4. اجرای بک‌تست با داده‌های دریافتی
    ↓
5. نمایش نتایج
```

## ⚠️ پیام‌های خطا

### اگر API key تنظیم نشده باشد:

```
❌ برای اجرای بک‌تست، لطفاً حداقل یک API key تنظیم کنید:
• Financial Modeling Prep: FINANCIALMODELINGPREP_API_KEY
• Twelve Data: TWELVEDATA_API_KEY

یا MetaTrader 5 را نصب و اجرا کنید (فقط برای Windows).
```

### اگر API در دسترس نباشد:

```
❌ نمی‌توان داده بازار را دریافت کرد.
لطفاً مطمئن شوید که حداقل یک API key تنظیم شده است:
Financial Modeling Prep (FINANCIALMODELINGPREP_API_KEY) یا
Twelve Data (TWELVEDATA_API_KEY).
MT5 در دسترس نیست.
```

## 🔍 بررسی وضعیت

### قبل از اجرای بک‌تست:

سیستم به طور خودکار بررسی می‌کند:
- ✅ آیا API key تنظیم شده است؟
- ✅ آیا API در دسترس است؟
- ✅ آیا می‌توان داده دریافت کرد؟

اگر همه چیز OK باشد، می‌توانید بک‌تست را اجرا کنید.

## 📊 مزایا

### ✅ برای کاربران موبایل:
- نیازی به نصب نرم‌افزار نیست
- فقط مرورگر کافی است
- کار می‌کند در: iOS, Android, Windows Mobile
- کار می‌کند در: Linux, Mac

### ✅ برای کاربران Windows بدون MT5:
- نیازی به نصب MT5 نیست
- فقط API key کافی است
- سریع‌تر از MT5 (نیازی به initialize نیست)

## 🚀 راه‌اندازی سریع

### 1. تنظیم API Key در سرور:

```bash
# در فایل .env
FINANCIALMODELINGPREP_API_KEY=CrFA9qczl3MRwERIiCGcmqloOilqkOBY
```

### 2. راه‌اندازی مجدد Backend (اگر نیاز باشد):

```bash
# اگر از Docker استفاده می‌کنید
docker-compose restart backend

# یا اگر مستقیم اجرا می‌کنید
# فقط .env را به‌روزرسانی کنید
```

### 3. تست:

1. از مرورگر موبایل یا هر دستگاهی به سایت بروید
2. استراتژی را آپلود کنید
3. به صفحه Testing بروید
4. استراتژی را انتخاب کنید
5. بک‌تست را اجرا کنید

## 🔄 مقایسه با MT5

| ویژگی | با MT5 | بدون MT5 (API) |
|------|--------|----------------|
| نصب نرم‌افزار | ✅ نیاز دارد | ❌ نیاز ندارد |
| پلتفرم | Windows فقط | همه پلتفرم‌ها |
| موبایل | ❌ کار نمی‌کند | ✅ کار می‌کند |
| سرعت | متوسط | سریع |
| نیاز به API | ❌ | ✅ |
| هزینه | رایگان | بسته به API |

## 📝 نکات مهم

1. **API Key را در سرور تنظیم کنید** (نه در موبایل)
2. **API Key باید معتبر باشد** (بررسی کنید که expire نشده باشد)
3. **برای استفاده production**، از plan پولی API استفاده کنید
4. **Rate Limiting**: برخی API ها محدودیت درخواست دارند

## 🐛 عیب‌یابی

### مشکل: "No data providers configured"

**راه حل:**
1. بررسی کنید که API key در `.env` تنظیم شده است
2. بررسی کنید که نام متغیر درست است: `FINANCIALMODELINGPREP_API_KEY`
3. Backend را restart کنید

### مشکل: "Provider returned no data"

**راه حل:**
1. API key را بررسی کنید (معتبر است؟)
2. اتصال اینترنت را بررسی کنید
3. Symbol را بررسی کنید (مثلاً XAU/USD یا EUR/USD)

### مشکل: "API rate limit exceeded"

**راه حل:**
1. چند دقیقه صبر کنید
2. یا از API key دیگری استفاده کنید
3. یا plan پولی API را خریداری کنید

## ✅ نتیجه

**اکنون تست استراتژی برای همه کاربران (موبایل، Linux, Mac, Windows بدون MT5) امکان‌پذیر است!**

فقط کافی است:
1. API key تنظیم شود (در سرور)
2. کاربر از مرورگر استفاده کند
3. استراتژی را آپلود و تست کند

**نیازی به MT5 نیست!** 🎉

