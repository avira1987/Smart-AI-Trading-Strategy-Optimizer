# راهنمای حل مشکلات رایج

## 🔴 مشکل: صفحه در حالت لودینگ می‌ماند

### علل احتمالی:
1. Backend در حال اجرا نیست
2. فایروال مسدود می‌کند
3. IP اشتباه است

### راه حل:
1. مطمئن شوید Backend روی `0.0.0.0:8000` در حال اجرا است
2. Console مرورگر را باز کنید (F12) و خطاها را بررسی کنید
3. IP محلی خود را بررسی کنید:
   ```powershell
   ipconfig | findstr IPv4
   ```
4. فایروال Windows را بررسی کنید

---

## 🔴 مشکل: خطای Google OAuth - origin_mismatch یا "Access blocked"

### ⚠️ مهم: Google شما را بلاک نکرده!
این فقط یک مشکل تنظیمات است که در چند دقیقه حل می‌شود.

### علت:
آدرس فعلی (IP شبکه محلی) در Google Cloud Console ثبت نشده است.

### راه حل سریع:
1. به [Google Cloud Console](https://console.cloud.google.com/) بروید
2. APIs & Services > Credentials
3. OAuth 2.0 Client ID خود را باز کنید
4. در بخش "Authorized JavaScript origins" این آدرس‌ها را اضافه کنید:
   ```
   http://localhost:3000
   http://127.0.0.1:3000
   http://YOUR_IP:3000
   ```
   (YOUR_IP را با IP محلی خود جایگزین کنید)
5. Save کنید و 1-2 دقیقه صبر کنید

### راهنمای کامل:
- **راهنمای گام به گام:** فایل `راهنمای_گام_به_گام_رفع_بلاک_Google.md` را مطالعه کنید
- **راهنمای تنظیمات:** فایل `راهنمای_تنظیم_Google_OAuth.md` را مطالعه کنید

---

## 🔴 مشکل: خطا در ارسال پیامک - "ارسال کننده نامعتبر است"

### علت:
شماره فرستنده (Sender) در Kavenegar تنظیم نشده است.

### راه حل سریع:
1. فایل `.env` را در ریشه پروژه باز کنید
2. این خط را اضافه/ویرایش کنید:
   ```env
   KAVENEGAR_SENDER=10008663
   ```
   (شماره خود را از پنل Kavenegar بگیرید)
3. Backend را راه‌اندازی مجدد کنید

### راهنمای کامل:
فایل `راهنمای_تنظیم_Kavenegar_SMS.md` را مطالعه کنید.

---

## 🔴 مشکل: سایت از دستگاه دیگر باز نمی‌شود

### بررسی‌ها:
1. ✅ Backend روی `0.0.0.0:8000` اجرا می‌شود؟
2. ✅ Frontend روی `0.0.0.0:3000` اجرا می‌شود؟
3. ✅ هر دو دستگاه در همان شبکه WiFi هستند؟
4. ✅ فایروال Windows باز است؟

### راه حل:
```powershell
# بررسی IP محلی
ipconfig | findstr IPv4

# باز کردن پورت‌ها در فایروال
netsh advfirewall firewall add rule name="Django Backend" dir=in action=allow protocol=TCP localport=8000
netsh advfirewall firewall add rule name="React Frontend" dir=in action=allow protocol=TCP localport=3000
```

---

## 🔴 مشکل: خطای CORS

### راه حل:
اگر در حالت DEBUG هستید، CORS باید به صورت خودکار همه origins را بپذیرد.
اگر مشکل ادامه داشت:
1. فایل `backend/config/settings.py` را بررسی کنید
2. مطمئن شوید `DEBUG = True` است
3. `CORS_ALLOW_ALL_ORIGINS = True` باید تنظیم شده باشد

---

## 📞 نیاز به کمک بیشتر؟

1. Console مرورگر را بررسی کنید (F12)
2. لاگ‌های Backend را بررسی کنید:
   - `backend/logs/api.log`
   - Terminal Backend
3. خطاها را کپی کنید و بررسی کنید

---

## ✅ چک‌لیست راه‌اندازی

- [ ] Backend روی `0.0.0.0:8000` اجرا می‌شود
- [ ] Frontend روی `0.0.0.0:3000` اجرا می‌شود
- [ ] Redis در حال اجرا است (برای Celery)
- [ ] فایل `.env` تنظیم شده است
- [ ] Google OAuth origins اضافه شده‌اند
- [ ] Kavenegar Sender تنظیم شده است
- [ ] فایروال پورت‌ها را باز کرده است

