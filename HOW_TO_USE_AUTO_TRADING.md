# راهنمای استفاده از سیستم معاملات خودکار

## 📋 مراحل راه‌اندازی و استفاده

### مرحله 1: آماده‌سازی محیط

#### 1.1. اجرای Redis
```bash
redis-server
```
یا اگر از Docker استفاده می‌کنید:
```bash
docker run -d -p 6379:6379 redis:7-alpine
```

#### 1.2. اجرای Migration
```bash
cd backend
python manage.py migrate
```

### مرحله 2: اجرای سرویس‌های لازم

برای کارکرد معاملات خودکار، باید 4 سرویس به صورت همزمان اجرا شوند:

#### Terminal 1: Celery Worker
```bash
cd backend
celery -A config worker --loglevel=info
```

#### Terminal 2: Celery Beat (برای اجرای دوره‌ای)
```bash
cd backend
celery -A config beat --loglevel=info
```

#### Terminal 3: سرور Django
```bash
cd backend
python manage.py runserver
```

#### Terminal 4: فرانت‌اند (اختیاری - فقط برای رابط کاربری)
```bash
cd frontend
npm run dev
```

### مرحله 3: اطمینان از اتصال MetaTrader 5

✅ **مهم**: قبل از فعال کردن معامله خودکار:
1. MetaTrader 5 را نصب و باز کنید
2. به حساب معاملاتی خود متصل شوید
3. مطمئن شوید MT5 باز و متصل است

### مرحله 4: استفاده از رابط کاربری

#### 4.1. ورود به صفحه معاملات زنده
- به آدرس `http://localhost:3000/trading` بروید
- یا از منوی ناوبری "معاملات زنده" را انتخاب کنید

#### 4.2. تنظیم معامله خودکار برای یک استراتژی

1. **انتخاب استراتژی:**
   - در بخش "تنظیمات معامله خودکار"، استراتژی مورد نظر را انتخاب کنید
   - فقط استراتژی‌های پردازش شده و فعال نمایش داده می‌شوند

2. **تنظیم پارامترها:**
   - **نماد**: نماد معاملاتی (پیش‌فرض: XAUUSD)
   - **حجم**: حجم هر معامله به لات (پیش‌فرض: 0.01)
   - **حداکثر معاملات باز**: تعداد معاملاتی که می‌تواند همزمان باز باشد (پیش‌فرض: 3)
   - **فاصله زمانی بررسی**: هر چند دقیقه یکبار استراتژی بررسی شود (پیش‌فرض: 5 دقیقه)
   - **حد ضرر**: مقدار حد ضرر به پیپ (پیش‌فرض: 50 پیپ)
   - **حد سود**: مقدار حد سود به پیپ (پیش‌فرض: 100 پیپ)
   - **ریسک در هر معامله**: درصد ریسک از سرمایه (پیش‌فرض: 2%)

3. **تست سیگنال:**
   - قبل از فعال کردن، دکمه "تست سیگنال" را بزنید
   - این دکمه نشان می‌دهد استراتژی در حال حاضر چه سیگنالی تولید می‌کند
   - می‌بینید: نوع سیگنال (خرید/فروش/نگاه داشتن)، سطح اعتماد، و دلیل

4. **ذخیره تنظیمات:**
   - دکمه "ذخیره تنظیمات" را بزنید

5. **فعال کردن معامله خودکار:**
   - دکمه "فعال کردن" را بزنید
   - وقتی فعال باشد، دکمه به رنگ سبز و "فعال" نمایش داده می‌شود

### مرحله 5: نظارت و مدیریت

#### 5.1. مشاهده معاملات
- در صفحه "معاملات زنده" می‌توانید:
  - معاملات باز را ببینید
  - قیمت‌های فعلی و سود/زیان را مشاهده کنید
  - معاملات بسته شده را بررسی کنید

#### 5.2. غیرفعال کردن معامله خودکار
- دکمه "غیرفعال کردن" را بزنید
- سیستم دیگر معامله جدیدی باز نمی‌کند (اما معاملات باز را مدیریت می‌کند)

#### 5.3. بستن دستی معاملات
- می‌توانید معاملات را به صورت دستی با دکمه "بستن" ببندید

### 🔄 نحوه کار سیستم

1. **هر 5 دقیقه (یا طبق تنظیمات شما):**
   - Celery Beat task `run_auto_trading` را اجرا می‌کند
   - برای هر استراتژی فعال:
     - داده‌های بازار را از MT5 می‌گیرد
     - استراتژی را روی داده‌ها اجرا می‌کند
     - سیگنال تولید می‌کند
     - اگر سیگنال مناسب باشد و شرایط برقرار باشد، معامله باز می‌کند

2. **مدیریت معاملات:**
   - قیمت‌های معاملات باز به‌روزرسانی می‌شود
   - Stop Loss و Take Profit به طور خودکار اعمال می‌شود
   - اگر معامله‌ای در MT5 بسته شود، در دیتابیس هم به‌روزرسانی می‌شود

### ⚙️ تنظیمات پیشرفته

#### تغییر فاصله زمانی بررسی
در تنظیمات می‌توانید `check_interval_minutes` را تغییر دهید.
**توجه**: کم کردن این مقدار ممکن است باعث تعداد زیاد درخواست به MT5 شود.

#### استفاده از Stop Loss و Take Profit
- می‌توانید استفاده از SL/TP را فعال یا غیرفعال کنید
- مقدار آن‌ها را به پیپ تنظیم کنید
- برای XAUUSD: 1 پیپ = 0.01 دلار

### 📊 مانیتورینگ

#### مشاهده لاگ‌ها
```bash
# لاگ Celery Worker
# در Terminal که worker را اجرا کرده‌اید مشاهده می‌کنید

# لاگ Celery Beat
# در Terminal که beat را اجرا کرده‌اید مشاهده می‌کنید
```

#### بررسی وضعیت در رابط کاربری
- در بخش "تنظیمات معامله خودکار" می‌توانید:
  - وضعیت فعال/غیرفعال را ببینید
  - زمان آخرین بررسی را مشاهده کنید
  - تنظیمات فعلی را ببینید

### ⚠️ نکات مهم

1. **MT5 باید همیشه باز باشد**
   - اگر MT5 بسته شود، سیستم نمی‌تواند معامله کند
   - اگر MT5 را ببندید و دوباره باز کنید، دکمه "همگام‌سازی موقعیت‌ها" را بزنید

2. **سرور باید همیشه روشن باشد**
   - برای معاملات 24/7، سرور باید همیشه روشن باشد
   - در Production از systemd یا supervisor استفاده کنید

3. **Redis باید در حال اجرا باشد**
   - اگر Redis خاموش شود، Celery کار نمی‌کند

4. **Celery Beat باید در حال اجرا باشد**
   - بدون Celery Beat، task های دوره‌ای اجرا نمی‌شوند

5. **بازار باید باز باشد**
   - سیستم خودکار بررسی می‌کند که بازار باز باشد
   - در آخر هفته (شنبه و یکشنبه) بازار بسته است

### 🐛 عیب‌یابی

#### معاملات باز نمی‌شوند
1. بررسی کنید MT5 باز و متصل است
2. بررسی کنید Celery Worker در حال اجرا است
3. بررسی کنید Celery Beat در حال اجرا است
4. بررسی کنید استراتژی فعال و پردازش شده است
5. بررسی کنید بازار باز است
6. از دکمه "تست سیگنال" استفاده کنید

#### خطای اتصال به MT5
- مطمئن شوید MT5 باز است
- مطمئن شوید به حساب معاملاتی متصل هستید
- دکمه "همگام‌سازی موقعیت‌ها" را بزنید

#### Celery کار نمی‌کند
- بررسی کنید Redis در حال اجرا است
- بررسی کنید Celery Worker و Beat در حال اجرا هستند
- لاگ‌ها را بررسی کنید

### 📝 مثال استفاده

```
1. MT5 را باز کنید و به حساب متصل شوید
2. Redis را اجرا کنید
3. Celery Worker را اجرا کنید
4. Celery Beat را اجرا کنید
5. Django را اجرا کنید
6. به صفحه معاملات زنده بروید
7. استراتژی را انتخاب کنید
8. تنظیمات را پر کنید (مثلاً: XAUUSD, 0.01 لات, SL: 50, TP: 100)
9. "تست سیگنال" را بزنید (برای اطمینان)
10. "ذخیره تنظیمات" را بزنید
11. "فعال کردن" را بزنید
12. حالا سیستم هر 5 دقیقه استراتژی را بررسی می‌کند و اگر سیگنال مناسب باشد، معامله باز می‌کند
```

### ✅ چک‌لیست قبل از فعال کردن

- [ ] MT5 باز و متصل است
- [ ] Redis در حال اجرا است
- [ ] Celery Worker در حال اجرا است
- [ ] Celery Beat در حال اجرا است
- [ ] Django در حال اجرا است
- [ ] استراتژی پردازش شده و فعال است
- [ ] تست سیگنال انجام شده و نتیجه مناسب است
- [ ] تنظیمات ذخیره شده است
- [ ] بازار باز است (نه آخر هفته)

پس از بررسی همه موارد، می‌توانید معامله خودکار را فعال کنید! 🚀

